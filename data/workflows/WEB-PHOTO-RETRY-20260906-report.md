# 同图改名重试冲突与历史照片收尾

## 定位

- 两个 Harness 上传入口曾使用仅含内容哈希的请求幂等键，但底层请求指纹还包含原始文件名。同一图片改名或重新粘贴后会产生 409。
- 文件存储本身已通过 `(user_id, purpose, content_sha256)` 唯一约束去重，无需在这两个内部入口叠加与文件名冲突的请求幂等键。
- 合成回归覆盖同图改名后的同步判题回执重放和异步批次重放；修改前两项均复现 409。
- 保留普通文件上传的显式请求幂等校验、账户隔离、内容哈希校验、冻结判题和复习阶段门禁；不改题目答案、不强制改数据库状态。
- 工程路由仅执行本地 `route --task web-implementation` 查询，未外发代码、图片、密钥或数据库记录做工程复核。
- 真实验收确认前两题恢复成功，剩余选择题暴露第二处问题：带选项参考的字母答案被无条件替换为选项内容参与比较，导致字母答案与自身校验冲突，被误报为参考变化。共享交叉校验现接受同一答案的字母或选项内容，错误字母、错误值、空答案和多选扩展仍拒绝；保留原有答案哈希及版本校验。

## 验证

- 修复后两项针对性回归通过；同图改名返回原批次、原候选和原回执，没有新增文件、判题或复习阶段。
- `.venv/Scripts/python.exe -X utf8 -B -m unittest discover -s tests -p "test_*.py"`：421 项通过（38.407 秒）；`git diff --check` 通过。
- 独立后台服务重启后 `/healthz` 返回 200。通过原 Web 会话完成真实图片处理：前两题复用已冻结正确候选和原提交时间，最后一题新增唯一正确候选；三题均关联原 PDF。只读核验 PDF checkpoint 的三条提交均为 `correct`，对应两组均有 `review_completed` 回执，没有重复创建错题。
- 选择题问题的单元回归和真实入口合成回归在修复前均复现失败；入口回归同时检查错误参考拒绝、正确参考提交及重放不重复创建候选。
- 最终全量命令同上：422 项通过（32.203 秒）。真实题库只读核对：相同参考字母返回 `consistent`，错误字母返回 `conflict`；未修改题库数据。
