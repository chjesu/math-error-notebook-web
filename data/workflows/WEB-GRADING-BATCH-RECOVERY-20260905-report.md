# 冻结判题批次恢复修复

## 证据与范围

- 本地会话工具记录显示首次提交完整 5 题，服务返回 409 后，模型连续尝试不同子集；旧错误只有通用顺序要求，没有当前未完成清单。
- Host 按图片保存已确认结果，失败后的未完成题集合可能变化；同轮重复转写会覆盖恢复状态。
- Harness 工具回执也使用 user role，不能将其当作真实用户消息查找附件。
- 本次只修复批次恢复契约，保留完整性、批次、会话、轮次及授权校验，不修改数学判定或用户数据库。
- 复用已有实现和测试，不新增依赖；未外发真实会话、图片或数据库内容。

## 验证

- 新增 3 张图片 / 5 道题合成回归：首张保存、次张 409；错误提供准确 pending_item_keys、completed_item_keys、batch_ref。
- 漏题、重复、倒序、越界、重交全批均在写入前拒绝，并返回可执行清单；照清单补交后每道题仅写一次。
- 同轮 items:[] 回读保持原批次、题干和已确认结果，不读图、不查题库、不创建新冻结版本。
- 验证 network / 非 JSON 响应的未确认语义，以及工具回执的 user role 不遮蔽真实附件；新的无图用户消息不回溯旧图。
- 局部测试：2 项批次契约测试、14 项 Harness runtime 测试通过。
- 全量命令：`.venv/Scripts/python.exe -X utf8 -B -m unittest discover -s tests -p "test_*.py"`，421 项通过（34.263 秒）。
- `git diff --check` 通过。未修改线上判定、清理数据库或宣称历史 409 的业务冲突已自动解决。
