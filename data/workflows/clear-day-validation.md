# 2970 本地会话及今日判题清理

用户授权：本轮明确要求“清空会话数据，清空今日判题信息”。
范围：当前已确认归属 2970 的 Harness 会话，及中国时间 2026-08-31 的判题。

## 只读盘点

- 今天 4 道判题、10 条候选版本、4 条题目识别记录、8 个识别/判题任务、4 条判题用量。
- 历史 PDF 中 3 条今日逐题作答；无今日整组复习完成记录、无今日新入本错题。清理后原复习任务阶段不应改变。
- 活跃 Harness 会话 1 个、该账号投影缓存 2 个；已确认 MySQL 会话绑定属于当前账号。
- 保留 60 条历史错题、62 份已完成 PDF、全部原复习任务、历史复习记录、原始文件、Token 历史计量和登录凭据。

## 执行边界

- 先停止已确认的 Web/Harness 进程避免新增写入；MySQL 保持运行。
- SQL 备份只导出将删除或修改的业务行，不导出认证凭据或密钥；会话目录和缓存备份到本地运行目录，不进入 Git。
- 在单个 MySQL 事务内按确认的账号、日期与资源编号删除，检查影响行数及历史数据指纹不变。发现新增入本、已推进阶段或范围变化则回滚，不猜测反向推进。
- 会话目录采用可恢复移出；只编辑已确认归属的 workspace/session 投影，不删除其他账号条目。
- 完成后重新启动服务，核对今日计数与会话列表、历史数据。
- 本次是用户授权的数据维护，不修改产品代码，不调用外发模型，不冒充独立批准人。

## 验证结果

- 已完成定点 SQL 备份与会话历史移出；备份位于 `data/runtime/clear-2970-20260831/`，受 Git 忽略规则保护，不进入仓库。
- 单事务已删除 4 次判题、10 条候选、4 条识别记录、8 个任务、4 条今日判题计量；清除旧 PDF 中 3 条今日逐题作答，无复习阶段回退或推进。
- 历史错题、复习任务、历史复习、原文件、模型计量及其他 PDF 行在事务前后逐表指纹一致；保留错题 60 条、PDF 62 份。
- 已确认归属的 Harness 历史会话移到备份；运行目录中会话文件 0，工作区会话列表与投影缓存均为 0。
- 服务已恢复，Web 健康/登录页面与 Harness UI 均 HTTP 200；本地数据库检查通过。
- 没有修改产品代码、执行外发模型或上传备份。
