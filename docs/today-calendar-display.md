# 学习进度：今日汇总

需求：今天始终显示今日到期、历史逾期、今日已完成；历史逾期可点开查看题干、错因、知识点、原到期日期及阶段，并勾选最多 12 道供今日复习。

复用现有 `/v1/progress/calendar`、`/v1/errors`、`/v1/progress` 的当前账号只读数据；今日汇总只在前端叠加，不改历史日期、月度统计、复习阶段或数据库。历史逾期包含以前月份未完成的任务，以中国标准时间的日期边界区分，不把今天稍早到期的题算作历史逾期。

选题草稿仅存当前浏览器标签页的 sessionStorage，按当前账号和中国日期隔离，复用错题本的 12 道上限和生成流程。刷新时再次过滤当前账户可用题目；不生成 PDF、不改动业务数据。存储不可用时明确提示，不声称已同步。

验证（2026-08-31）：

- 完整回归 `python -X utf8 -B -m unittest discover -s tests -p "test_*.py"`：264 项通过；`node --check web/app.js` 通过。
- 前端纯逻辑和模拟 DOM 事件测试覆盖：中国日期零点、跨月逾期、空数据显式显示 0、账号与日期隔离、历史日期和月统计不变、展开错因与知识点、最多勾选 12 道、刷新恢复选题、存储失败明确提示。
- 本地服务重启后，8000 健康检查及 3080 Harness 页面均返回 200。
- 浏览器检查被 URL 安全策略拦截，未完成修改后的真实页面点击和视觉验收；没有使用其他浏览器或绕过策略。上述自动化测试不等同于浏览器验收。
- 未生成 PDF，未修改错题、复习任务或复习历史数据。

此任务仅涉及本地界面，不代表独立安全审查或生产部署批准。
